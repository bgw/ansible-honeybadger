## This file is autogenerated by ansible-honeybadger. Do not modify it directly.
## Instead, update the template in ansible-honeybadger, and redeploy. Maybe
## submit a pull request?

[Service]
Nice={{ tor_nice }}

{% if tor_procname != "tor" %}
ExecStart=
ExecStart=@/usr/bin/tor {{ tor_procname }} \
    --defaults-torrc /usr/share/tor/tor-service-defaults-torrc \
    -f /etc/tor/torrc \
    --RunAsDaemon 0
Environment=LD_PRELOAD=/opt/procname/libprocname.so
Environment=PROCNAME={{ tor_procname }}
{% endif %}
