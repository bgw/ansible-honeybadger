#jinja2: trim_blocks: False
// This file is autogenerated by ansible-honeybadger. Do not modify it directly.
// Instead, update the template in ansible-honeybadger, and redeploy. Maybe
// submit a pull request?

// use origin:archive pairs
Unattended-Upgrade::Allowed-Origins {
    "${distro_id}:${distro_codename}";
    "${distro_id}:${distro_codename}-security";
    "${distro_id}:${distro_codename}-updates";
    {% if tor %}"TorProject:${distro_codename}";{% endif %}
    {% if bitcoind %}"LP-PPA-bitcoin-bitcoin:${distro_codename}";{% endif %}
};

Unattended-Upgrade::Package-Blacklist { };

Unattended-Upgrade::AutoFixInterruptedDpkg "true";
Unattended-Upgrade::Remove-Unused-Dependencies "false";
Unattended-Upgrade::Automatic-Reboot "true";
Unattended-Upgrade::Automatic-Reboot-Time "now";
Unattended-Upgrade::MinimalSteps "true"; // allow shutdowns during upgrades

// we don't have a mailserver, so this won't work
//Unattended-Upgrade::Mail "root";
//Unattended-Upgrade::MailOnlyOnError "false";
